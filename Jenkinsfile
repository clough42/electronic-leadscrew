pipeline {
	agent any
	
	stages {
		stage('Build Debug') {
			steps {
				echo 'Building debug...'
			}
		}
		stage('Build Release') {
			steps {
				echo 'Building release...'
			}
		}
	}
}